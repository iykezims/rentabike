<p id="notice"><%= notice %></p>
<fieldset>
<h6>Search Profile</h6>
<%#= form_tag(Profile_path, :method => "get", firstname: "search-form") do %>
<%#= text_field_tag :search, params[:search], placeholder: "search Users By Firstname" %>
<%#= submit_tag "search" %>
<%# end %>

<%= form_tag(profiles_path, :method => "get", firstname: "Search-form") do %>
<%= text_field_tag :search, params[:search], placeholder: "Search profile by Firstname" %>
<%= submit_tag "Search" %>
<% end %>
<br>
</fieldset><br>

<h1>Profiles</h1>
<br><hr color="green"><br>

<table>
  <thead>
    <tr>
      <th>Firstname</th>
      <th>Lastname</th>
      <th>Address</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profiles.each do |profile| %>
      <tr>
        <td><%= profile.firstname %></td>
        <td><%= profile.lastname %></td>
        <td><%= profile.address %></td>
        <td><%= profile.user %></td>
        <td><%= link_to 'Show', profile %></td>
        <td><%= link_to 'Edit', edit_profile_path(profile) %></td>
        <td><%= link_to 'Destroy', profile, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Profile', new_profile_path %>
